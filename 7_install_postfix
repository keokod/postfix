apt-get install postfix
apt-get install postfix-mysql
apt-get install libsasl2-modules
apt-get install libsasl2-modules-sql

//install postfixadmin
apt-get install postfix
Postfix Configuration ::  Site Internet  
Postfix Configuration :: stackod.com
 │ Faut-il configurer la base de données de postfixadmin avec                │ 
 │ dbconfig-common ?  :: oui
  Mot de passe de connexion MySQL pour postfixadmin : root
   │ Méthode de connexion pour la base de données MySQL de postfixadmin:: scoket unix
   Identifiant MySQL pour postfixadmin :: postfixadmin@localhost
   Nom de la base de données MySQL pour postfixadmin : postfixadmin
   Postfix Configuration::
   
   │ Autres destinations pour lesquelles le courrier sera accepté (champ vide autorisé) : 
    pandan.stackod.com, smtp.stackod.com, imap.stackod.com, localhost.fr, localhos
    
